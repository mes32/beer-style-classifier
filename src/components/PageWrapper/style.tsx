import { FunctionComponent } from 'react';
import styled from 'styled-components';

const PageWrapperStyle: FunctionComponent = styled.div`
    margin: 0;
    padding: 1rem 40px;
`;

export default PageWrapperStyle;